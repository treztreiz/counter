{% extends 'base.html.twig' %}

{% block title %}Editer le Projet{% endblock %}

{% block body %}

<nav>
    <div class="nav-wrapper container row">
        <div class="col s12">
            <a href="{{ path('project_index') }}" class="breadcrumb"><i class="material-icons left">folder</i>Mes
                projets</a>
            <a href="{{ path('project_show', { id: project.id }) }}" class="breadcrumb">{{ project.name }}</a>
        </div>
    </div>
</nav>

<main class="wrapper" style="background:{{ project.color }};">
    <div class="container">
        <div class="row">
            <div class="col s12">

                <div class="header">
                    <h1>Editer le projet</h1>
                    {{ include('project/_delete_form.html.twig') }}
                </div>

                {{ include('project/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            </div>
        </div>
    </div>
</main>

{% endblock %}

{% block javascripts %}
<script src="{{asset('js/color.js')}}"></script>
{% endblock %}